// Admin panel: its pages (public/) plus the admin backend (backend/index.js).
// Deploy from this folder with `npx wrangler deploy` (Cloudflare project root
// directory: admin), on the admin subdomain. Deploy fruit-shop first.
{
  "$schema": "../node_modules/wrangler/config-schema.json",
  "name": "fruit-shop-admin",
  "main": "backend/index.js",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "public",
    "binding": "ASSETS"
  },
  // Calls the customer site's backend (fruit-shop) directly inside Cloudflare.
  "services": [{ "binding": "SHOP_API", "service": "fruit-shop" }],
  // Set in the dashboard and kept between deploys:
  //   ADMIN_PASSWORD, SESSION_SECRET, INTERNAL_API_KEY (secrets)
  //   SHOP_URL (optional text, the shop's address for the "Open customer site" link)
  "keep_vars": true,
  "observability": { "enabled": true }
}
